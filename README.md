Hello! Welcome to my rice! This started on swayfx, but has since moved to Hyprland, so if you want to use swayFx, you'll need to update the config with stuff like default apps and running programs on startup.

TO INSTALL: Most of the folders can go in ~/.config, wallpapers MUST be in ~/Pictures. Eww needs a bit more setup: the config file must be revised, since images paths are hardcoded with my user name. Why it only works hardcoded I will never know.
